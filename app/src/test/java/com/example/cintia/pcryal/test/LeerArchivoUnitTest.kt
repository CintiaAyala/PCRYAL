package com.example.cintia.pcryal.test

import android.os.Environment
import android.os.Environment.getExternalStorageDirectory
import com.example.cintia.pcryal.Utility
import org.junit.Test

import org.junit.Before
import org.junit.rules.TemporaryFolder
import org.junit.Rule
import org.mockito.Mockito
import java.io.File
import org.mockito.Mock
import org.mockito.Mockito.mock
import org.junit.runner.RunWith;
import org.mockito.junit.MockitoJUnitRunner;
import org.mockito.Mockito.`when`

class LeerArchivoUnitTest {
 /*   @Rule
    var storageDirectory = TemporaryFolder()
    private var existentDirectory: File? = null
    var utility = Utility()

    @Mock
    var env: Environment? = null
    @Before
    fun setup() {
       //env = mock(Environment::class.java)
        `when`(getExternalStorageDirectory().absolutePath)
                .thenReturn(storageDirectory.toString())
    }


    @Test
    fun leerArchivoTest(){
        var fileName = "equipos.json"
        var strArchivo = utility.leerArchivos(fileName)

        //Assert.assertEquals(existentDirectory.getAbsolutePath() + File.separator + fileName, logFilePath)
    }*/
}